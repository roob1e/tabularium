export interface Student {
    id: number;
    fullname: string;
    age: number;
    phone: string;
    birthdate: string;
    groupName: string;
}

export interface Group {
    id: number;
    name: string;
    amount: number;
}
